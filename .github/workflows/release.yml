# .github/workflows/release.yml
name: "release & publish"

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Гілка з якої робимо реліз"
        default: "main"
        required: false

env:
  PUSH_BRANCH: ${{ inputs.branch || 'main' }}

jobs:
  # ──────────────────────────────── 1. PREPARE ────────────────────────────────────
  prepare:
    name: "🔧 Підготовка"
    runs-on: ubuntu-latest
    steps:
      - name: 🛎️ Checkout ${{ env.PUSH_BRANCH }}
        uses: actions/checkout@v4
        with:
          ref: ${{ env.PUSH_BRANCH }}
          fetch-depth: 0

      - name: 🔧 Встановити Go 1.22
        uses: actions/setup-go@v5
        with:
          go-version: 1.22

      - name: 📥 Зчитати версію з build-скриптів
        id: ver
        shell: bash
        run: |
          set -Eeuo pipefail
          nameBuild=$(./_run/scripts/sys.sh -n)
          versionBuild=$(./_run/scripts/sys.sh -v)

          # зберігаємо також у файл для наступних job’ів
          echo "BUILD_NAME=$nameBuild"   >  build_env.txt
          echo "BUILD_VER=$versionBuild" >> build_env.txt

          echo "name=$nameBuild"   >> $GITHUB_OUTPUT
          echo "version=$versionBuild" >> $GITHUB_OUTPUT

          echo "✔️  Знайдено версію: $versionBuild (імʼя пакету: $nameBuild)"

      - name: 🚀 Опублікувати build-env артефакт
        uses: actions/upload-artifact@v4
        with:
          name: build-env
          path: build_env.txt

  # ──────────────────────────────── 2. BUILD ───────────────────────────────────────
  build:
    name: "🛠️ Збірка [${{ matrix.os }}]"
    needs: prepare
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, windows-latest ]
        go: [ 1.22 ]

    steps:
      - name: 🛎️ Checkout ${{ env.PUSH_BRANCH }}
        uses: actions/checkout@v4
        with:
          ref: ${{ env.PUSH_BRANCH }}
          fetch-depth: 0

      - name: 📥 Download env
        uses: actions/download-artifact@v4
        with:
          name: build-env
          path: .

      - name: 🗒️ Source env
        shell: bash
        run: |
          set -Eeuo pipefail
          cat build_env.txt >> $GITHUB_ENV

      - name: 🔧 Встановити Go ${{ matrix.go }}
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go }}

      - name: 🗂️ Кеш модулів Go
        uses: actions/cache@v4
        with:
          path: |
            ${{ runner.os == 'Windows' && 'C:\Users\runneradmin\go\pkg\mod' || '~/.cache/go-build' }}
            ${{ runner.os == 'Windows' && 'C:\Users\runneradmin\go\bin' || '~/go/bin' }}
          key: ${{ runner.os }}-go-${{ hashFiles('go.mod') }}
          restore-keys: ${{ runner.os }}-go

      - name: 🛠️ go build
        shell: bash
        run: |
          set -Eeuo pipefail
          
          mkdir -p target
          mkdir -p tmp
          
          go generate
          go mod tidy
          
          go build -ldflags="-s -w" -o treelang ./cli/...

      - name: 🏷️ Перейменувати білд + розширення
        shell: bash
        run: |
          set -Eeuo pipefail
          if [[ "${{ runner.os }}" == "Windows" ]]; then
            mv treelang treelang.${{ env.BUILD_VER }}.windows.exe
          else
            mv treelang treelang.${{ env.BUILD_VER }}.linux.bin
            chmod +x treelang.${{ env.BUILD_VER }}.linux.bin
          fi

      - name: 📦 Завантажити білд як артефакт
        uses: actions/upload-artifact@v4
        with:
          name: bin-${{ runner.os }}
          path: treelang.*

  # ─────────────────────────────── 3. RELEASE ──────────────────────────────────────
  release:
    name: "🚀 Публікація релізу"
    needs: [ build ]
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: 🛎️ Checkout ${{ env.PUSH_BRANCH }}
        uses: actions/checkout@v4
        with:
          ref: ${{ env.PUSH_BRANCH }}
          fetch-depth: 0

      - name: 📥 Download env
        uses: actions/download-artifact@v4
        with:
          name: build-env
          path: .

      - name: 🗒️ Source env
        shell: bash
        run: cat build_env.txt >> $GITHUB_ENV

      - name: 📥 Download ALL binaries
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: 🏷️ Створити commit без «важкого» та tag ${BUILD_VER}
        id: tag
        shell: bash
        run: |
          set -Eeuo pipefail
          ORIG_SHA=$(git rev-parse HEAD)

          # 1️⃣ Прибираємо зайві шляхи лише з індексу, а не з робочої директорії
          git rm -r --cached localization localization_go _examples || true
          git rm --cached $(git ls-files '*.bin' '*.md') || true
          git rm -r --cached .github || true

          # 2️⃣ Коміт та тег
          git commit -m "release(${BUILD_VER}): strip heavy assets"
          git tag -a "${BUILD_VER}" -m "Release ${BUILD_VER}"
          git push origin "${BUILD_VER}"

          # 3️⃣ Повертаємо repo до початкового стану
          git reset --hard "$ORIG_SHA"

      - name: 🚀 Створити реліз
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.BUILD_VER }}
          name: ${{ env.BUILD_VER }}
          generate_release_notes: true
          files: |
            dist/**
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: ✔️ Готово
        run: echo "🎉 Реліз $BUILD_VER успішно опубліковано!"
